import 'dart:math';

void main() {
  ///Math Operator
  /// +,-,*,/,%
  /// (operator)=
  /// ++,--
  // int a = 1;
  // int b = 2;
  // var total = a * b;
  // print(total);

  // double a = 1;
  // a %= 2; //  1/2 = 0.5

  // print(a);
  /// 2 * 2
  // print(pow(2, 2));
}
